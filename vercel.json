{
  "buildCommand": "pnpm --filter @workspace/codestarix run build && cp -r artifacts/codestarix/dist/public dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
